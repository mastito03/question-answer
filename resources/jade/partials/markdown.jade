include ../_mixin

+sectionshow('style')
    link(rel="stylesheet" href!="{{ url('css/bootstrap-markdown/bootstrap-markdown.min.css') }}")

+sectionshow('script')
    script(src!="{{ url('js/bootstrap-markdown/bootstrap-markdown.js') }}")
    script.
        $('#markdown').markdown({
            autofocus:false,
            savable:false,
            resize:'vertical',
            fullscreen:false,
        });